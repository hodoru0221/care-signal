# WiFi Sensing 병실 모니터링 시스템 설계안

## 1. 1차 구현 목표

1차 시연에서는 다음 네 상태를 실시간으로 구분한다.

- `EMPTY`: 측정 구역에 사람이 없음
- `IN_BED`: 환자가 침대에 있음
- `OUT_OF_BED`: 환자가 침대에서 이탈함
- `MOVEMENT_ANOMALY`: 급격한 움직임 후 정지 등 이상 움직임 의심

`MOVEMENT_ANOMALY`는 낙상 확정이 아닌 확인이 필요한 이상 징후로 취급한다.

## 2. 전체 구성

```text
[ESP32-S3 송신기]
       │ 일정 주기의 WiFi 패킷
       ▼
[ESP32-S3 CSI 수신기]
       │ USB Serial: CSI 원시 데이터
       ▼
[PC 수집 프로그램]
       │ 전처리된 시간 구간(window)
       ▼
[분류 모델]
       │ 상태 + 신뢰도
       ▼
[FastAPI 서버 + 메모리/PostgreSQL JSONB 스냅샷]
       ├── HTTP 폴링 ──> [병동 웹 대시보드]
       ├── HTTP API  ──> [보호자 웹·Expo 앱]
       └── HTTP API  ──> [UNO R4 WiFi + 스피커]
```

개발 단계에서는 PC 한 대에서 수집 프로그램, 모델과 API 서버를 실행한다. 최종 구조에서는 병원 내부 PC가 모델 결과를 공개 HTTPS API로 전송하며, 병원 웹·보호자 웹·보호자 앱이 같은 공개 서버를 사용한다. 병원 내부 경보는 인터넷 장애 시에도 로컬에서 계속 동작하고, 외부 전송 결과는 임시 저장 후 재전송한다.

## 3. 하드웨어 구성

### 3.0 현재 보유 수량과 권장 배치

| 보유 장치 | 수량 | 1차 배치 |
|---|---:|---|
| 전용 ESP32 개발보드 | 0 | 추후 CSI 송신·수신용으로 2대 확보 권장 |
| Arduino UNO R4 WiFi | 3 | 병실 알림기 1, 간호사실 알림기 1, 예비·개발기 1 |
| LM386 스피커 모듈 | 2 | 병실 알림기 1, 간호사실 알림기 1 |
| USB A-B 케이블 | 2 | UNO R4 프로그래밍·전원용 |
| USB A-C 케이블 | 2 | ESP32 연결용. 실제 보드 단자 확인 필요 |
| 아크릴 고정판 | 2 | CSI 송신기와 수신기 위치 고정 |
| microSDXC 256GB | 1 | 현재는 PC 저장용으로 보류 |

UNO R4 WiFi에 ESP32-S3가 내장되어 있지만 기본적으로 RA4M1의 무선 통신 보조 역할을 한다. 1차 설계에서는 이를 전용 CSI 개발보드 수량으로 계산하지 않는다. 전용 ESP32-S3 DevKit을 확보하면 아래 구성을 그대로 사용한다.

microSD 카드는 보드에 microSD 슬롯이나 별도 SD 모듈이 없으면 직접 장착할 수 없다. 1차 구현에서는 CSI 원본을 PC에 저장하므로 필수 부품이 아니다.

### 3.1 CSI 송신기

- 권장 장치: ESP32-S3 DevKit 1대
- 역할: 고정된 WiFi 채널에서 일정 주기로 패킷 전송
- 설치 위치: 침대 한쪽 벽 또는 침대 머리 방향
- 전원: USB 전원

### 3.2 CSI 수신기

- 권장 장치: ESP32-S3 DevKit 1대
- 역할: 수신 패킷의 CSI, RSSI, 타임스탬프를 USB Serial로 PC에 전달
- 설치 위치: 송신기 반대편, 신호 경로가 침대 영역을 지나도록 고정
- 출력 형식: 한 줄에 한 패킷을 기록하는 JSON Lines 또는 CSV

예시:

```json
{"timestamp":1710000000123,"seq":125,"rssi":-47,"csi":[3,-2,5,-1]}
```

### 3.3 UNO R4 WiFi 알림 장치

- 역할: 서버의 현재 경보 상태를 1초 간격으로 확인
- 출력: LED 상태 표시 및 LM386 스피커 안내음
- 정상: 무음
- 주의: 짧고 작은 안내음 1회
- 긴급: 반복음. 간호사가 확인하면 즉시 중지
- 안전 원칙: 큰 소리로 낙상을 단정하는 음성은 사용하지 않음

두 알림기의 역할을 구분한다.

- 병실 알림기: 낮은 음량의 안내음, 장치 상태 LED
- 간호사실 알림기: 긴급 경보음, 해당 병실 번호 또는 경보 패턴 표시
- 예비 UNO R4: 펌웨어 개발과 교체 테스트에 사용

LM386 모듈은 증폭기이므로 실제 음원 입력 방식과 전원 전압, 입력 핀 구성을 제품 사양에서 확인한 뒤 UNO R4에 연결한다. 초기 시험은 낮은 음량으로 시작한다.

### 3.4 설치 원칙

- 송신기, 수신기, 침대 위치와 높이를 테이프로 표시한다.
- 실험 중 공유기 채널과 송수신기 위치를 변경하지 않는다.
- 주변 사람의 이동을 실험 메모에 기록한다.
- 실제 환자가 아닌 팀원이 안전한 동작만 수행한다.

## 4. 소프트웨어 구성

### 4.1 기술 선택

- 백엔드: Python FastAPI
- 화면 갱신: 브라우저의 주기적 HTTP API 조회
- 데이터 저장: 로컬은 메모리, 배포는 PostgreSQL JSONB 스냅샷
- 프론트엔드: 의존성 없는 HTML/CSS/JavaScript와 Expo 앱
- 모델 연결: 병원 PC 게이트웨이가 장치 키로 추론 API 호출
- CSI 원본 저장: CSV 또는 Parquet

FastAPI는 모델·게이트웨이와 같은 Python 생태계에서 연동한다. 데이터베이스 URL이 없으면 메모리 저장소를 사용하고, `NEON_DATABASE_URL` 또는 `DATABASE_URL`이 있으면 상태·사건·이력을 하나의 원자적 JSONB 스냅샷으로 저장한다. 로그인 세션은 데이터베이스가 아니라 서버 메모리에 유지된다.

### 4.2 백엔드 모듈

```text
backend/
  main.py                API, 인증, 정적 웹 제공
  domain.py              병실 상태, 사건, 이력과 스냅샷
  ward.py                정적 병동 배치와 상태 결합
  persistence.py         PostgreSQL JSONB 스냅샷 저장
  notifications.py       보호자 Expo 푸시 메시지
  simulator.py           시연용 상태 입력
gateway/
  uploader.py            추론 결과 전송과 실패 큐 재전송
web/
  index.html             직원 병동 맵 대시보드
  guardian.html          보호자 모바일 웹
```

### 4.3 병동 맵 데이터 구조

`backend/ward.py`의 정적 `WARD_LAYOUT`은 병동 `id`·`name`, `rooms[]`, `stations[]`를 정의한다. 병실 항목은 `room_id`, 표시명, 침상명과 CSS 그리드 좌표를 가진다. `GET /api/v1/ward/map`은 각 정적 병실에 `MonitoringStore.rooms()`의 실시간 `status`를 결합한다.

모니터링 스냅샷은 `rooms`(병실별 최신 상태), `events`(사건 ID별 대응 상태), `history`(병실 ID별 감지 기록) 세 객체로 구성된다. 구형 스냅샷에 `history`나 새 병실이 없으면 불러올 때 빈 이력과 기본 병실 상태를 보완한다. 이력은 병실마다 최대 500건을 저장하고 API에서는 최신순으로 최대 200건까지 반환한다.

### 4.4 프론트엔드 화면

한 화면에서 다음 정보를 보여준다.

- 장치 연결 상태: 송신기/수신기/분류 서버/UNO 알림기
- 현재 환자 상태와 마지막 갱신 시간
- 위험도와 모델 신뢰도
- 최근 CSI 변화 그래프
- 활성 알림 목록
- `확인`, `이동 중`, `처리 완료`, `오탐` 버튼
- 최근 사건과 대응 시간 기록

## 5. 컴포넌트 간 상호작용

### 5.1 정상 상태 처리

1. 수신기가 CSI 패킷을 PC로 보낸다.
2. 수집기가 일정 길이의 window를 만든다.
3. 모델이 상태와 신뢰도를 반환한다.
4. 서버가 상태를 즉시 저장하고 위험 상태로 바뀐 경우 활성 중복 사건이 없을 때 사건을 만든다.
5. 웹은 HTTP API를 다시 조회해 최신 상태를 표시한다.

### 5.2 이상상황 처리

1. 모델이 `OUT_OF_BED` 또는 `MOVEMENT_ANOMALY`를 반환한다.
2. 서버가 상태를 `WARNING` 또는 `CRITICAL`로 매핑한다.
3. 같은 병실·같은 유형의 활성 사건이 없으면 사건을 생성한다.
4. 웹 대시보드에 경보가 표시된다.
5. UNO R4가 서버에서 경보를 읽고 안내음을 출력한다.
6. 담당자가 웹에서 `확인`을 누르면 경보음이 중지된다.
7. `처리 완료` 또는 `오탐` 결과와 대응 시간이 저장된다.

### 5.3 현재 프로토타입 경보 규칙

| 조건 | 등급 | 동작 |
|---|---|---|
| `OUT_OF_BED` 입력 | 주의 | 웹 주황색, 병실 장치는 `GENTLE_ONCE` |
| `MOVEMENT_ANOMALY` 입력 | 긴급 의심 | 웹 빨간색, 장치는 `URGENT_REPEAT` |
| 담당자가 `ACKNOWLEDGED` 처리 | 확인 | 장치 경보음 중지, 확인 시각 저장 |
| `RESPONDING` 처리 | 이동 중 | 보호자 화면에 직원 확인 중 표시 |
| `COMPLETED` 또는 `FALSE_ALARM` 처리 | 종료 | 완료 시각 저장, 활성 사건에서 제외 |

마지막 관측 수신 후 30초가 지나면 장치를 오프라인으로 표시하는 입력 끊김 감지는 구현되어 있다. 지속시간 필터와 30초 재알림은 실제 모델 정확도·오탐률 측정 후 값을 확정해 적용할 향후 규칙이다.

## 6. API 초안

### 상태 조회

`GET /api/v1/rooms/{room_id}/status`

```json
{
  "room_id": "room-01",
  "state": "IN_BED",
  "confidence": 0.91,
  "risk_level": "NORMAL",
  "updated_at": "2026-07-18T14:30:00+09:00"
}
```

### UNO R4 경보 조회

`GET /api/v1/devices/{device_id}/alert`

```json
{"level":"WARNING","sound":true,"event_id":"evt-103"}
```

### 사건 상태 변경

`PATCH /api/v1/events/{event_id}`

```json
{"status":"ACKNOWLEDGED","actor":"nurse-01"}
```

### 병동 맵과 이력

- `GET /api/v1/ward/map`: 직원 인증 후 정적 배치와 전체 병실의 현재 상태 반환
- `GET /api/v1/rooms/{room_id}/history?limit=30`: 직원 인증 후 병실 감지 이력을 최신순 반환

현재 웹 화면은 WebSocket이 아니라 이 HTTP API들을 주기적으로 조회한다.

## 7. 자동화 QA

테스트 전용 의존성은 `requirements-test.txt`에 정의한다.

```powershell
pip install -r requirements-test.txt
python -m unittest discover -s tests -v
```

`tests/test_api.py`는 직원·보호자 인증 분리, 병동 맵 계약, 여러 병실의 독립 상태와 사건, 이력 API, 요청 검증을 HTTP 수준에서 확인한다. `tests/test_domain.py`는 사건 중복 방지, 경보 중지, 스냅샷 복원, 구형 스냅샷 보완, 병실별 이력 격리를 확인한다.

## 8. 개발 단계

### 1단계: 하드웨어 없이 통합

- 가짜 CSI 및 가짜 분류 결과 생성기
- FastAPI 상태/사건 API
- 웹 대시보드
- 확인 및 처리 완료 흐름

### 2단계: 실제 CSI 수집

- ESP32-S3 송수신기 구성
- Serial 수집 및 원본 저장
- 실험 메타데이터 기록

### 3단계: 모델 연결

- 전처리 규격 확정
- 모델 출력 형식 통일
- 이벤트 엔진의 지속시간과 임계값 조정

### 4단계: 알림 하드웨어 연결

- UNO R4 서버 연결
- LM386 음량 및 경보 패턴 검증
- 웹 확인 버튼과 경보 중지 연동

### 5단계: 통합 시연

- 재실, 침대 이탈, 이상 움직임 시나리오
- 장치 연결 끊김 시나리오
- 탐지 시간, 오탐, 알림 확인 시간 측정

## 9. 역할 간 전달 규격

- 데이터팀 → 모델팀: 원본 CSI 파일과 실험 메타데이터
- 모델팀 → 웹팀: `state`, `confidence`, `timestamp` 형식의 결과
- 웹팀 → 하드웨어팀: 경보 조회 API 주소와 JSON 형식
- 하드웨어팀 → 웹팀: 장치 heartbeat와 마지막 연결 시각

서로의 내부 코드를 직접 참조하지 않고 위 데이터 형식만 지키도록 한다.

## 10. 1차 완료 기준

- CSI 데이터가 PC에 연속 저장된다.
- 네 상태 중 하나가 1초 이내 주기로 웹에 갱신된다.
- 이상상황 생성 후 웹 경보와 스피커가 동작한다.
- 웹에서 확인하면 스피커가 중지된다.
- 사건 발생, 확인, 완료 시각이 기록된다.
- 센서 단절이 환자 이상상황과 별도로 표시된다.
